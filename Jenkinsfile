pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                \\ command to compile the Calc and CalcTest classes (surrounded in the bat ' ... ' command for windows, sh ' ... ' for anything else)
            }
        }

        stage('test') {
            steps {
                \\ command to run CalcTest (surrounded in the bat ' ... ' command for windows, sh ' ... ' for anything else)
            }
        }
    }
}
